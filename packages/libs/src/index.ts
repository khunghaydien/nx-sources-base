/**
 * @nx-sources-base/libs - UI + API (axios) + React Query dùng chung cho tất cả dự án.
 * Trong app dùng alias ngắn: import từ '@ui', '@api', '@query'.
 */
export * from './ui';
export * from './api';
export * from './query';
